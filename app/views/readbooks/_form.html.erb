<%= form_with(model: readbook, local: true) do |form| %>
  <% if readbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(readbook.errors.count, "error") %> prohibited this readbook from being saved:</h2>

      <ul>
      <% readbook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_field :review %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
